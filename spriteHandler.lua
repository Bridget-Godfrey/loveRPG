-- spriteHandler.lua
-- make spritesheets an object, allowing for similar handling of animations, static images, and dynamic stuff?


function newSprite(spritesheet, scaleX, scaleY, spriteW, spriteH, quadX, quadY, fps, xOffset, yOffset, flags, animationGroups)
	local s = {}
	return s
end




function newStaticImage(spritesheet, quadX, quadY, scaleX, scaleY, spriteW, spriteH, marginX, marginY, xOffset, yOffset)
	local s = {}
	return s
end